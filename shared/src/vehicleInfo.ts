export interface VehicleInfo {
  id: string;
  latitude: number | null;
  longitude: number | null;
  name: string | null;
  vehicleType: string | null;
  status: string | null;
  route: string | null;
}
